# 可插拔 Skills 通用 Agent Client 进度文档

## 1. 文档目标

本文件用于持续记录项目开发进度，作为开发执行层文档。

用途包括：

- 记录当前阶段开发计划
- 记录每一轮实际完成内容
- 记录当前存在的问题和风险
- 记录下一轮开发 TODO

后续开发必须同时遵循以下文档：

- [设计文档](/Users/a1021500406/private/uni-agent/docs/设计文档.md)
- [开发文档](/Users/a1021500406/private/uni-agent/docs/开发文档.md)
- [进度文档](/Users/a1021500406/private/uni-agent/docs/进度文档.md)

## 2. 当前总计划

### Phase 1：最小可用 Client

目标：

- 完成 Python CLI 工程骨架
- 完成 Skill 加载和匹配基础能力
- 完成 Tool Registry
- 完成本地受控 Sandbox 雏形
- 完成最小可运行的 Agent Runtime

### Phase 2：真实 Agent 执行闭环

目标：

- 接入 `PydanticAI`
- 实现真实 Planner
- 实现 Tool Registry 到执行器的映射
- 让 `agent run` 可以完成真实工具调用

### Phase 3：可观测与回放

目标：

- 增加任务日志
- 增加会话和执行回放
- 增加错误归因信息

### Phase 4：安全增强与容器化

目标：

- 增强本地沙盒策略控制
- 演进到 Docker 容器沙盒
- 增加更细粒度的权限控制

## 3. 当前状态概览

- 当前阶段：Phase 1
- 当前状态：进行中
- 当前重点：从骨架阶段进入真实执行链路

## 4. 开发记录

### Round 1

时间：

- 2026-03-12

目标：

- 完成项目定位收敛
- 完成文档初始化
- 完成 Python 工程骨架

完成项：

- 明确项目不是平台，而是 Agent Client
- 完成设计文档
- 完成开发文档
- 增加进度文档
- 初始化 `pyproject.toml`
- 初始化 `src/uni_agent` 包结构
- 实现 `typer` CLI 基础入口
- 实现 `SkillLoader`
- 实现 `SkillMatcher`
- 实现 `ToolRegistry`
- 实现 `LocalSandbox` 雏形
- 实现 `Orchestrator` skeleton
- 增加示例 Skill
- 增加基础单元测试
- 清理 Python 缓存文件并推送到 GitHub

产出文件：

- [README.md](/Users/a1021500406/private/uni-agent/README.md)
- [docs/设计文档.md](/Users/a1021500406/private/uni-agent/docs/设计文档.md)
- [docs/开发文档.md](/Users/a1021500406/private/uni-agent/docs/开发文档.md)
- [docs/进度文档.md](/Users/a1021500406/private/uni-agent/docs/进度文档.md)

当前问题：

- `PydanticAI` 尚未接入真实 planner
- 工具执行器尚未接通
- `agent replay` 尚未实现
- 本机因为 Python SSL 证书问题，依赖安装和 pytest 运行未完成

验证情况：

- `python3 -m compileall src` 已通过
- `pip install -e '.[dev]'` 因本机 SSL 证书问题失败
- `pytest` 因依赖未安装未执行

## 5. 下一轮 TODO

### Round 2 目标

- 接入 `PydanticAI`
- 将 `Orchestrator` 改造为真实 Planner + Executor
- 为 `shell_exec`、`file_read`、`search_workspace` 增加真实执行器
- 增加运行日志落盘
- 增加 `agent replay` 命令骨架

### Round 2 TODO 清单

- 定义 `LLMProvider` / planner 接口
- 接入 `PydanticAI` agent
- 定义工具调用到执行器的统一协议
- 为 Sandbox 增加更严格的命令策略
- 增加任务执行结果存储目录
- 增加集成测试样例

## 6. 记录规范

每完成一轮开发，都要在本文件追加一个新的 `Round N` 记录，至少包含：

- 时间
- 本轮目标
- 实际完成项
- 当前问题
- 验证情况
- 下一轮 TODO

## 7. 开发约束

后续开发必须遵循：

- 设计文档：定义系统目标、边界和架构原则
- 开发文档：定义技术栈、模块拆解、实现步骤和测试项
- 进度文档：定义当前阶段计划、已完成内容和下一轮开发目标

如果实现内容与文档不一致，应先更新文档，再继续开发。
