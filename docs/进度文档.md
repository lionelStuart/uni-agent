# 可插拔 Skills 通用 Agent Client 进度文档

## 1. 文档目标

本文件用于持续记录项目开发进度，作为开发执行层文档。

用途包括：

- 记录当前阶段开发计划
- 记录每一轮实际完成内容
- 记录当前存在的问题和风险
- 记录下一轮开发 TODO

后续开发必须同时遵循以下文档：

- [设计文档](./设计文档.md)
- [开发文档](./开发文档.md)
- [进度文档](./进度文档.md)

## 2. 当前总计划

### Phase 1：最小可用 Client

目标：

- 完成 Python CLI 工程骨架
- 完成 Skill 加载和匹配基础能力
- 完成 Tool Registry
- 完成本地受控 Sandbox 雏形
- 完成最小可运行的 Agent Runtime

### Phase 2：真实 Agent 执行闭环

目标：

- 接入 `PydanticAI`
- 实现真实 Planner
- 实现 Tool Registry 到执行器的映射
- 让 `agent run` 可以完成真实工具调用

### Phase 3：可观测与回放

目标：

- 增加任务日志
- 增加会话和执行回放
- 增加错误归因信息

### Phase 4：安全增强与容器化

目标：

- 增强本地沙盒策略控制
- 演进到 Docker 容器沙盒
- 增加更细粒度的权限控制

## 3. 当前状态概览

- 当前阶段：**Phase 2 已完成主要闭环**，Phase 3（可观测、回放、结论）**部分落地**
- 当前状态：**可交付日常使用**（LLM 规划 + 多轮重规划 + 沙箱策略 + 流式事件 + 运行结论 + **可配置 LLM 系统提示词** + **记忆目录与「回忆身份」类任务规划已对齐** + **Codex/Cursor 风格 `SKILL.md` 与 references/scripts 自动加载并注入规划提示**）
- 当前重点：Skills 冲突与计划文件 schema、可选「恢复即成功」编排策略、`PydanticAIPlanner.run_sync` 异常时统一回退启发式、容器沙盒（Phase 4）

## 4. 开发记录

### Round 1

时间：

- 2026-03-12

目标：

- 完成项目定位收敛
- 完成文档初始化
- 完成 Python 工程骨架

完成项：

- 明确项目不是平台，而是 Agent Client
- 完成设计文档
- 完成开发文档
- 增加进度文档
- 初始化 `pyproject.toml`
- 初始化 `src/uni_agent` 包结构
- 实现 `typer` CLI 基础入口
- 实现 `SkillLoader`
- 实现 `SkillMatcher`
- 实现 `ToolRegistry`
- 实现 `LocalSandbox` 雏形
- 实现 `Orchestrator` skeleton
- 增加示例 Skill
- 增加基础单元测试
- 清理 Python 缓存文件并推送到 GitHub

产出文件：

- [README.md](../README.md)
- [docs/设计文档.md](./设计文档.md)
- [docs/开发文档.md](./开发文档.md)
- [docs/进度文档.md](./进度文档.md)

当前问题：

- `PydanticAI` 尚未接入真实 planner
- 工具执行器尚未接通
- `agent replay` 尚未实现
- 本机因为 Python SSL 证书问题，依赖安装和 pytest 运行未完成

验证情况：

- `python3 -m compileall src` 已通过
- `pip install -e '.[dev]'` 因本机 SSL 证书问题失败
- `pytest` 因依赖未安装未执行

### Round 2

时间：

- 2026-03-12

目标：

- 将 `Orchestrator` 从 skeleton 推进到最小可运行闭环
- 为核心内置工具接入真实执行器
- 增加任务日志落盘和 `agent replay` 命令骨架
- 建立本地虚拟环境并跑通测试

完成项：

- 更新 `README` 开发流程，明确每轮开发前创建功能分支、结束后合并回 `main`
- 为 `PlanStep` 和 `TaskResult` 增加执行参数、步骤输出、`run_id` 和错误字段
- 增加 `TaskRunRecord` 模型
- 新增 `Planner` 接口和 `HeuristicPlanner` 默认实现
- 新增 `Executor`，接通计划执行主链路
- 将 `Orchestrator` 改造为 `planner + executor + task_store` 结构
- 为 `shell_exec`、`file_read`、`search_workspace` 接入真实执行 handler
- 为 `LocalSandbox` 增加输出截断能力
- 新增任务日志落盘目录配置 `task_log_dir`
- 新增 `TaskStore`，将任务结果保存到 `.uni-agent/runs`
- 新增 `agent replay` CLI 命令骨架
- 新增运行时与任务存储单元测试
- 创建本地 `.venv` 虚拟环境
- 安装项目依赖并跑通测试

产出文件：

- [README.md](../README.md)
- [src/uni_agent/agent/planner.py](../src/uni_agent/agent/planner.py)
- [src/uni_agent/agent/executor.py](../src/uni_agent/agent/executor.py)
- [src/uni_agent/agent/orchestrator.py](../src/uni_agent/agent/orchestrator.py)
- [src/uni_agent/tools/builtins.py](../src/uni_agent/tools/builtins.py)
- [src/uni_agent/tools/registry.py](../src/uni_agent/tools/registry.py)
- [src/uni_agent/sandbox/runner.py](../src/uni_agent/sandbox/runner.py)
- [src/uni_agent/observability/task_store.py](../src/uni_agent/observability/task_store.py)
- [src/uni_agent/cli/main.py](../src/uni_agent/cli/main.py)
- [src/uni_agent/config/settings.py](../src/uni_agent/config/settings.py)
- [src/uni_agent/shared/models.py](../src/uni_agent/shared/models.py)
- [tests/unit/test_orchestrator.py](../tests/unit/test_orchestrator.py)
- [tests/unit/test_task_store.py](../tests/unit/test_task_store.py)

当前问题：

- `PydanticAI` 仍未接入真实 planner，本轮使用的是本地 `HeuristicPlanner`
- 当前仅接通了 `shell_exec`、`file_read`、`search_workspace`，其他内置工具还没有真实执行器
- `agent replay` 目前只支持读取任务结果，尚未支持完整执行回放
- 当前环境不允许直接写 `.git/refs`，本轮无法在沙盒内实际创建功能分支
- 依赖安装需要通过 `pip --trusted-host` 绕过本机 SSL 证书问题

验证情况：

- `python3 -m compileall src tests` 已通过
- 在 `.venv` 中执行 `python -m pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org -e '.[dev]'` 已通过
- 在 `.venv` 中执行 `python -m pytest -q` 已通过，结果为 `6 passed in 0.11s`

### Round 3

时间：

- 2026-04-14

目标：

- 接入 `PydanticAI` 结构化规划，并在无密钥时自动回退
- 补齐 `file_write` / `http_fetch` 执行器与更严格的本地命令策略
- 增强步骤级错误归因与 `agent replay` 可读输出

完成项：

- 定义 `LLMProvider` 协议与 `EnvLLMProvider`
- 新增 `PydanticAIPlanner`（`output_type=LLMStructuredPlan`，`defer_model_check` 便于测试与延迟校验）
- CLI 支持 `UNI_AGENT_PLANNER_BACKEND`：`auto` / `heuristic` / `pydantic_ai`（默认 `auto`：有 `OPENAI_API_KEY` 或 `ANTHROPIC_API_KEY` 时走 LLM，否则启发式）
- 为 `file_write`、`http_fetch` 接入真实 handler（`http_fetch` 支持最大字节截断，URL 仅限 `http/https`）
- `LocalSandbox` 默认命令白名单为 `pwd,ls,cat,echo,rg`（**Round 6 起默认含 `du`**，与 `DEFAULT_SANDBOX_ALLOWED_COMMANDS` 对齐），并可通过 `UNI_AGENT_SANDBOX_ALLOWED_COMMANDS` 覆盖
- `PlanStep` 增加 `error_type` / `error_detail`，`Executor` 在失败时写入，`TaskResult.error` 优先使用 `error_detail`
- `agent replay` 增加 `-v/--verbose`，按步骤输出状态与错误归因后再打印完整 JSON
- 新增单元测试与 `tests/integration/test_agent_run.py`（`file_write` 端到端）

当前问题：

- LLM 规划仍依赖外部 API 与网络；结构化计划与任务语义的匹配质量需结合实际任务继续调参
- `HeuristicPlanner` 仍未覆盖写入类自然语言任务，复杂任务需依赖 LLM planner 或显式计划
- `agent replay` 仍为“日志回放 + 步骤摘要”，尚未支持对历史计划自动重执行

验证情况：

- `python3 -m compileall src tests` 已通过
- 在 `.venv` 中执行 `python -m pytest -q` 已通过，结果为 `12 passed`

### Round 4

时间：

- 2026-04-14

目标：

- 提升启发式 Planner 对写入与拉取类任务的覆盖
- 收紧 `http_fetch` 的网络暴露面并统一沙箱超时配置
- 为 LLM Planner 与 CLI 回放补充可配置项与可脚本化输出

完成项：

- `HeuristicPlanner` 支持最小 `file_write` 句式与任务中的 `http(s)` URL 触发 `http_fetch`；当写入路径与读取路径冲突时跳过多余的 `file_read`
- 新增 `http_fetch` URL 安全校验（私网/回环/链路本地/metadata 等，默认拒绝；`UNI_AGENT_HTTP_FETCH_ALLOW_PRIVATE_NETWORKS=true` 可放行）
- `LocalSandbox` 支持 `command_timeout`，与 `UNI_AGENT_SANDBOX_COMMAND_TIMEOUT_SECONDS` 对齐；`http_fetch` 复用同一超时配置
- `PydanticAIPlanner` 支持 `UNI_AGENT_PLANNER_INSTRUCTIONS`、`UNI_AGENT_LLM_TEMPERATURE`、`UNI_AGENT_LLM_RETRIES`
- `agent replay` 支持 `--format steps|jsonl|full`（`jsonl` 末行输出 `task_result` 摘要）
- 增补启发式规划、`http_fetch` 策略、`file_read` 路径穿越与 CLI `jsonl` 回放等测试；集成测试改为真实启发式写入句式

当前问题：

- 启发式写入/抓取仍依赖固定句式，复杂自然语言仍需 LLM Planner
- `http_fetch` 未做 DNS 重绑定防护；仅基于 URL 主机字面量做基线拦截
- Executor 尚未实现失败后的自动重试或局部重规划

验证情况：

- `python3 -m compileall src tests` 已通过
- 在 `.venv` 中执行 `python -m pytest -q` 已通过，结果为 `25 passed`

### Round 5

时间：

- 2026-04-14

目标：

- 为工具执行增加可配置重试与结构化失败码
- 提供静态计划文件入口以绕过弱启发式
- 为 `http_fetch` 增加可选域名白名单，并补充多 Skill 工具并集行为的测试覆盖

完成项：

- `Executor` 支持 `max_step_retries`（`UNI_AGENT_TOOL_STEP_RETRIES`），失败时在单步骤内重试后再中止整条计划
- `PlanStep.failure_code`：`sandbox_error` / `unknown_tool` / `invalid_arguments` / `tool_execution_error`
- 新增 `load_plan_file`（`.yaml` / `.yml` / `.json`）；`agent run "<label>" --plan path` 跳过自动规划
- `http_fetch` 支持 `UNI_AGENT_HTTP_FETCH_ALLOWED_HOSTS`（逗号分隔；支持 `.suffix` 后缀规则）
- 新增计划加载、域名白名单与「多 Skill 工具并集限制 file_write」等单元测试

当前问题：

- 重试策略尚不区分可恢复/不可恢复错误，可能造成无效重复调用
- 多 Skill 场景当前是工具并集语义，尚未提供「求交」或显式冲突提示
- 计划文件缺少 schema 版本与校验钩子

验证情况：

- `python3 -m compileall src tests` 已通过
- 在 `.venv` 中执行 `python -m pytest -q` 已通过，结果为 `32 passed`

### Round 6

时间：

- 2026-04-14

目标：

- 多轮编排与失败重规划、沙箱交互式批准、搜索/磁盘类任务体验、流式执行可见性、运行结论文本、仓库忽略运行日志

完成项：

- **Orchestrator**：失败后在 `prior_context` 下重试规划；`UNI_AGENT_ORCHESTRATOR_MAX_FAILED_ROUNDS`；步骤 id 前缀 `r{n}-`；**最终成功条件**为 `plan` 中**全部**步骤 `completed`（历史中若有 `failed` 则整单 `failed`）
- **Planner**：`create_plan(..., prior_context=...)`；**HeuristicPlanner** 识别「最大文件夹/磁盘用量」类中文与英文描述，优先 `shell_exec` + `du -h -d 1 .`（兼容 macOS/BSD，避免 `-sh` 与 `-d` 同用）；此类任务不再误走整句 `search_workspace`
- **Sandbox**：`approve_non_allowlisted` 回调；TTY 下对非白名单命令询问一次批准（`Y`）；`UNI_AGENT_SANDBOX_PROMPT_FOR_DISALLOWED`；**默认白名单**含 `du`（`DEFAULT_SANDBOX_ALLOWED_COMMANDS`，与 `LocalSandbox` / Pydantic 提示同源）
- **search_workspace**：`query` 空白归一化（避免 prior 日志换行破坏 ripgrep）；`rg` 将退出码 **0/1**（无匹配）视为成功
- **PydanticAIPlanner**：`shell_exec` 校验 argv 形态与禁止 shell 元字符；`UNI_AGENT_OPENAI_*` 与网关 Qwen profile（`tool_choice` 与思考模式兼容）
- **流式输出**：`Orchestrator.stream_event` 推送 NDJSON 语义事件；`Executor.on_step_complete`；`agent run` 默认 `--stream`（**stderr** 一行一 JSON），`--no-stream` 关闭；**stdout** 仍为最终完整 `TaskResult` JSON
- **结论**：`TaskResult.conclusion`；确定性摘要 + 可选 LLM 合成（`UNI_AGENT_RUN_CONCLUSION_LLM`）；提示词要求与日志一致、避免自相矛盾
- **仓库**：扩展 `.gitignore`；**不再跟踪** `.uni-agent/runs/*.json`（运行日志仅本地）

当前问题：

- 严格「全步骤成功」在多轮补救场景下仍可能整单 `failed`（产品可后续提供「以最后一轮为准」等策略）
- 重试策略仍不区分可恢复/不可恢复错误
- 计划文件仍无 schema 版本字段；`http_fetch` 无 DNS 重绑定防护

验证情况：

- `python -m pytest -q`：**51 passed**（以当前仓库为准）

### Round 7

时间：

- 2026-04-14

目标：

- 将 LLM 侧「系统提示词」产品化：可配置、有统一默认文案、与规划器/结论合成对齐

完成项：

- 新增 `src/uni_agent/agent/system_prompts.py`：集中 **`DEFAULT_PLANNER_SYSTEM_PROMPT`**、**`DEFAULT_CONCLUSION_SYSTEM_PROMPT`**，以及 `effective_planner_instructions` / `effective_conclusion_instructions`（支持可选全局前缀拼接）
- **Settings**：`UNI_AGENT_GLOBAL_SYSTEM_PROMPT`（`global_system_prompt`）— 加在规划器与结论系统提示前；`UNI_AGENT_CONCLUSION_SYSTEM_PROMPT`（`conclusion_system_prompt`）— 覆盖结论默认；沿用 `UNI_AGENT_PLANNER_INSTRUCTIONS`（`planner_instructions`）覆盖规划器默认
- **接线**：`PydanticAIPlanner` 与 `RunConclusionSynthesizer` 读取上述配置；`build_orchestrator` 统一注入
- **默认规划提示**：补充「纯闲聊类任务可单步 `shell_exec` + `echo`」指引，降低非工具任务的结构化输出失败率
- **文档**：`.env.example` 增加系统提示相关变量说明
- **测试**：`tests/unit/test_system_prompts.py`

产出文件（节选）：

- [src/uni_agent/agent/system_prompts.py](../src/uni_agent/agent/system_prompts.py)
- [src/uni_agent/config/settings.py](../src/uni_agent/config/settings.py)（`global_system_prompt` / `conclusion_system_prompt`）
- [src/uni_agent/agent/pydantic_planner.py](../src/uni_agent/agent/pydantic_planner.py)、[src/uni_agent/agent/run_conclusion.py](../src/uni_agent/agent/run_conclusion.py)、[src/uni_agent/cli/main.py](../src/uni_agent/cli/main.py)
- [tests/unit/test_system_prompts.py](../tests/unit/test_system_prompts.py)
- [.env.example](../.env.example)、[README.md](../README.md)、[docs/开发文档.md](./开发文档.md)

当前问题：

- `PydanticAIPlanner.create_plan` 在 `run_sync` 因输出校验耗尽重试时仍会抛异常（尚未统一 `try/except` 回退启发式）；与 Round 6 的「空计划回退」互补，需后续收口

验证情况：

- `python -m pytest -q`：**61 passed**（以当前仓库为准）

### Round 8

时间：

- 2026-04-14

目标：

- 修复「有记忆文件但检索/规划像失效」：记忆路径与 workspace 不一致、以及 LLM 规划将「我是谁」等误判为纯闲聊
- 强化 `memory_search` 在关键词 LLM 失败时的可用性

完成项：

- **Settings**：`UNI_AGENT_MEMORY_DIR` 未设置时默认 **`$UNI_AGENT_WORKSPACE/.uni-agent/memory`**；相对路径按 **workspace** 解析（不再绑定进程 cwd，避免读写与检索目录分叉）
- **`run_memory_search_llm`**：关键词规划 LLM 抛错时回退为仅用用户 `query` 参与关键词归一化，避免整段检索静默失败
- **规划 / 意图路由**：`HeuristicPlanner._memory_search_query` 增加 **回忆身份/自我** 类短语（如 `我是谁`、`我叫什么`、`还记得我吗`、`who am i` 等），命中则单步 `memory_search`（`query` 为整句任务）
- **`PydanticAIPlanner`**：在调用规划 LLM **之前**复用上述 `_memory_search_query` 短路，避免模型选 `shell_exec`+`echo` 绕过记忆
- **默认规划系统提示**（`DEFAULT_PLANNER_SYSTEM_PROMPT`）：明确要求身份/过往自述类问题 **必须先 `memory_search`**；将「闲聊用 echo」收窄为**非**回忆用户已保存事实的场景
- **文档与示例**：`.env.example` 注明记忆目录与 workspace 关系；新增 `tests/unit/test_settings_memory_dir.py`；补充启发式 / LLM 规划短路相关单测

当前问题：

- `PydanticAIPlanner.create_plan` 在 `run_sync` 因输出校验耗尽重试时仍会抛异常（尚未统一 `try/except` 回退启发式）；与「空计划回退」互补，需后续收口
- 回忆类短语列表仍为手工维护；复杂表述可能仍需依赖 LLM 规划或用户显式「搜索记忆：…」

验证情况：

- `python -m pytest -q`：**72 passed**（以当前仓库为准）

### Round 9

时间：

- 2026-04-15

目标：

- 从 `skills_dir` 自动加载全部 skill，兼容 **Codex / Claude Code / Cursor** 的 `SKILL.md` + references + scripts

完成项：

- **`SkillLoader`**：遍历 `skills_dir` 下一层子目录；存在 **`SKILL.md`** 则按 frontmatter + 正文加载，否则 **`skill.yaml`**；同目录二者并存时合并元数据（`skill.yaml` 可补充 `allowed_tools`、`version`、`triggers` 等）
- **`skills/bundle`**：`references/`、`reference.md`、`examples.md`、`scripts/` 发现与 **`instruction_text`** 合成（小参考文件可内联；超长整体截断）
- **`SkillSpec`**：新增 `skill_load_format`、`instruction_text`、`reference_paths`、`script_paths`
- **`SkillMatcher`**：`triggers` 为空时用 `description` 拆词匹配（便于仅含 `name`/`description` 的 Codex 风格 skill）
- **`PydanticAIPlanner`**：将选中技能的 `instruction_text` 注入用户提示
- **CLI**：`SkillLoader(settings.skills_dir, settings.workspace)` 以生成相对 workspace 的路径提示
- **测试**：`tests/unit/test_skill_bundle.py`；`test_skill_loader` 断言示例 skill 的 `instruction_text`

当前问题：

- **启发式规划器**仍不读取 `instruction_text`（仅 LLM 规划受益）；脚本执行仍受沙箱白名单约束，需在 skill 中说明或申请批准
- skill 目录在 workspace 外时，路径提示为绝对路径，`file_read` 可能仍受 workspace 限制

验证情况：

- `python -m pytest -q`：**76 passed**（以当前仓库为准）

## 5. 下一轮 TODO

### Round 10 目标

- 产品化 Skills 冲突：提示、求交模式或优先级裁决配置
- 为计划文件增加版本字段与校验报告
- 可选编排策略：末轮成功即标 `completed` 或显式配置
- `PydanticAIPlanner`：`run_sync` 失败（含输出校验超限）时回退 `HeuristicPlanner`

### Round 10 TODO 清单

- 为 `Executor`/`Orchestrator` 增加可插拔的 `RetryPolicy` 或错误分类器（区分可重试错误）
- 扩展 `agent replay` 支持重执行（dry-run / 实际执行开关）
- 文档化当前「同步单线程执行」与何时引入异步的判据
- 为 `http_fetch` 增加可选解析期 DNS 策略说明与测试替身
- LLM **生成过程**流式（与当前「执行过程」流式区分）按需接入

## 6. 记录规范

每完成一轮开发，都要在本文件追加一个新的 `Round N` 记录，至少包含：

- 时间
- 本轮目标
- 实际完成项
- 当前问题
- 验证情况
- 下一轮 TODO

## 7. 开发约束

后续开发必须遵循：

- 设计文档：定义系统目标、边界和架构原则
- 开发文档：定义技术栈、模块拆解、实现步骤和测试项
- 进度文档：定义当前阶段计划、已完成内容和下一轮开发目标

如果实现内容与文档不一致，应先更新文档，再继续开发。
